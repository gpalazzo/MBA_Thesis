logreg_params:
  # utilizar esses parâmetros independentemente de otimização
  model_params_padrao:
    n_jobs: -1
    random_state: 0
  # parâmetros default da biblioteca:
  # - fit_intercept = True
  # - class_weight = None
  # - max_iter = 100
  model_params_extra:
    solver: "saga"
    penalty: "l1"
    tol: 0.00001
    C: 339.3221771895323
    # l1_ratio: 0.1

# ANALISAR OS RESULTADOS COM ESSES PARAMETROS
# {'C': 339.3221771895323,
#  'class_weight': 'balanced',
#  'fit_intercept': True,
#  'penalty': 'l2',
#  'solver': 'saga',
#  'tol': 0.001}
